<% greetings = ["Hello","Welcome back", "How you doin'"] %>

<div id="top-user-bar">
	<% if current_user %>
		<div id="top-bar-buttons" class="show">
	    	<%= greetings.sample %> <%= link_to current_user.username, user_path(current_user) %> <% if user_admin? current_user %> ADMIN!! <%end%> | <%= link_to "Log out", log_out_path %>
	    </div>
	<% else %>
		<% if content_for? :top_user_bar %>
			<div id="top-bar-buttons" class="show">
				<%= yield :top_user_bar %>
		    </div>

		<%else %>
			<div id="top-bar-buttons" class="show">
	    		<%= link_to "Sign up", sign_up_path %> or 
	    		<%= link_to "Log in", log_in_path, id: "top-bar-login-button" %>
	    	</div>
	 	<% end %>
	<% end %>

	<div id="top-bar-auth" class="hidden">
		<%= form_tag log_in_path do %>
		    <%= text_field_tag :email, params[:email], placeholder: "username or email" %>
		    <%= password_field_tag :password,  "", placeholder: "password" %>
		  	<%= submit_tag "Log in" %>
			<span class="close">[<a href="#" title="Close Log in">x</a>]</span>
		<% end %>
	</div>
</div>